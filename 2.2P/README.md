# SIT725 Task 2.2P – Express Web Server

## Overview

This project demonstrates a basic Node.js and Express web server.

It includes:

- A webpage served from the `public` folder
- A simple GET endpoint
- A REST API endpoint that adds two numbers
- Input validation for invalid values

## Installation

Open a terminal in the `2.2P` folder and install the dependencies:

```bash
npm install